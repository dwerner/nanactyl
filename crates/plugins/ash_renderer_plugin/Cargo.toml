[package]
name = "ash_renderer_plugin"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
render = {path = "../../render" }
models = { path = "../../models" }
world = {path = "../../world" }

# workspace
ash = { workspace = true }
bytemuck = { workspace = true }
