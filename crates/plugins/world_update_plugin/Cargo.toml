[package]
name = "world_update_plugin"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]

plugin_self = { path = "../../plugin_self" }
input = { path = "../../input" }
world = { path = "../../world" }
logger = { path = "../../logger" }
glam = { workspace = true }
