[package]
name = "world_update_plugin"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
input = { path = "../../input" }
world = { path = "../../world" }
logger = { path = "../../logger" }
glam = { workspace = true }
